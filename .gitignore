# Gradle
.gradle/
build/

# xcodebuild derivedData (로컬 빌드 산출물)
build-rel/
build_rel/
GL_IOS/build_rel/
GL_IOS/build_release/
build_dev/
GL_IOS/build_dev/

# Android / Studio
.idea/
local.properties
*.iml
*.hprof
/captures
.externalNativeBuild
.cxx

# OS
.DS_Store
Thumbs.db

# Firebase config (개발자별로 직접 추가 — 커밋 금지)
GL_Android/google-services.json

# 릴리스 서명 키스토어 — 절대 커밋 금지(유출 시 앱 위장 서명 가능). local.properties 에 경로/비번 보관
*.keystore
*.jks

# Claude / local agent config
.claude/

# 프로젝트 문서·장애 리포트 (로컬 참고용 — 커밋 금지, iCloud 원본 + 로컬 심볼릭 링크)
GL_MD
GL_MD/
GatchaLOG_Error Report/

# iOS / Xcode (KMP 마이그레이션)
GL_IOS/GL_IOS.xcodeproj/xcuserdata/
GL_IOS/GL_IOS.xcodeproj/project.xcworkspace/xcuserdata/
*.xcuserstate
DerivedData/

# Kotlin 2.x 로컬 캐시
.kotlin/

# iOS IPA 빌드 산출물
GL_IOS/build/
build_reldev/
GL_IOS/build_reldev/
build_sim/
GL_IOS/build_sim/
# Xcode 27(iOS 27 SDK) 전용 derivedData — 26 툴체인 산출물(build/)과 분리해 둔다
build_x27/
GL_IOS/build_x27/
# 같은 27 툴체인의 Release 구성(실기기 릴리즈 검증용) — Debug 산출물과 분리
build_x27_rel/
GL_IOS/build_x27_rel/

# Firebase CLI 작업 로그
.firebase/

# 에디터·SourceKit LSP 로컬 설정 (.idea/ · .claude/ 와 같은 취급 — 머신마다 다르다)
# buildServer.json 은 xcode-build-server 가 생성하며 절대 경로와 DerivedData 해시가 박힌다.
.vscode/
buildServer.json
